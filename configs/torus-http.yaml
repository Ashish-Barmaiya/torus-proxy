apiVersion: v1

server:
  addr: ":8080"

health:
  interval_ms: 5000
  timeout_ms: 2000
  path: /health

routes:
  - path: /api
    upstream:
      - "http://backend-1:3001"
      - "http://backend-2:3002"
